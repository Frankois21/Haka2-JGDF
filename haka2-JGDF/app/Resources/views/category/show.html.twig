{% extends 'base.html.twig' %}


{% block body %}

    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
    <h1>Categorie {{ category.category }}</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td><h1>{{ category.category }}</h1></td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('category_index') }}" class="btn btn-default">Retour à la liste</a>

            <a href="{{ path('category_edit', { 'id': category.id }) }}" class="btn btn-default">Edit</a>

            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-danger" value="Supprimer">
            {{ form_end(delete_form) }}


            </div>
        </div>
    </div>

{% endblock %}